# 侧栏间距控制指南

本文档详细说明 Vocabulary 页面侧栏各个部分之间的间距在哪里调整。

---

## 📐 侧栏结构概览

```
┌─────────────────────────────────┐
│  *单词学习*                      │ ← 标题（由 Typst 生成）
│  ↓ 0.6rem                       │
│  ┌─────────────────────────┐   │
│  │ 今日进度                 │   │
│  │ ━━━━━━━━━━━━━━━━━━━━━  │   │
│  │ 7/20 词                  │   │
│  └─────────────────────────┘   │
│  ↓ 0.8rem                       │
│  ┌─────────────────────────┐   │
│  │ 本周统计                 │   │
│  │ • 新学    7 词           │   │
│  │ • 复习    0 词           │   │
│  │ • 掌握率  0%             │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
↓ 由 Tufted 自动控制（约 2-3rem）
┌─────────────────────────────────┐
│  *考研倒计时*                    │
│  ↓ 0.5rem                       │
│  331天 13时 57分 22秒           │
└─────────────────────────────────┘
↓ 1.5rem (todo-margin-note)
┌─────────────────────────────────┐
│  *待办事项*                      │
│  ↓ 0.6rem                       │
│  ┌─────────────────────────┐   │
│  │ 分组                     │   │
│  │ To-do...            [+] │   │
│  │ □ 优化博客页面           │   │
│  └─────────────────────────┘   │
└─────────────────────────────────┘
↓ 1.5rem (data-management-margin-note)
┌─────────────────────────────────┐
│  *数据管理*                      │
│  ↓ 0.6rem                       │
│  ┌──────────┬──────────┐       │
│  │↓导出数据 │↑导入数据 │       │
│  └──────────┴──────────┘       │
└─────────────────────────────────┘
```

---

## 🎯 间距控制位置详解

### 1️⃣ 侧栏之间的垂直间距

#### A. Tufted 主题默认间距
**位置**: `assets/tufted.css` (Tufted 主题自动控制)
**说明**: 相邻的 `margin-note` 之间的默认间距由 Tufted 主题自动管理，通常为 2-3rem

**如果需要修改全局默认间距**，可以在 `assets/tufted.css` 中添加：
```css
.marginnote + .marginnote {
    margin-top: 2rem; /* 调整这个值 */
}
```

#### B. 特定侧栏的额外间距
**位置**: `assets/pomodoro-todo.css` 和 `assets/vocabulary.css`

**待办事项侧栏**:
```css
/* 文件: assets/pomodoro-todo.css */
.todo-margin-note {
    margin-top: 1.5rem !important;  /* ← 调整这里 */
}
```

**数据管理侧栏**:
```css
/* 文件: assets/vocabulary.css */
.data-management-margin-note {
    margin-top: 1.5rem !important;  /* ← 调整这里 */
}
```

---

### 2️⃣ 单词学习侧栏内部间距

**文件**: `assets/vocabulary.css`

#### A. 标题到内容的间距
```css
.vocab-sidebar-wrapper {
    margin-top: 0.6rem;  /* ← 标题下方间距 */
}
```

#### B. 今日进度区域
```css
.vocab-progress-section {
    display: block;
    margin-bottom: 0.8rem;  /* ← 与下一个区域的间距 */
}

.vocab-progress-label {
    margin-bottom: 0.3rem;  /* ← 标签与进度条的间距 */
}

.vocab-progress-bar {
    margin-bottom: 0.3rem;  /* ← 进度条与文字的间距 */
}
```

#### C. 本周统计区域
```css
.vocab-stats-section {
    padding-top: 0.8rem;     /* ← 顶部内边距 */
    margin-top: 0.8rem;      /* ← 顶部外边距 */
    border-top: 1px solid var(--fg-muted);  /* 分隔线 */
}

.vocab-stats-title {
    margin-bottom: 0.5rem;   /* ← 标题与列表的间距 */
}

.vocab-stat-item {
    margin-bottom: 0.3rem;   /* ← 统计项之间的间距 */
}
```

---

### 3️⃣ 考研倒计时侧栏内部间距

**文件**: `assets/pomodoro-todo.css`

#### A. 标题到内容的间距
```css
.countdown-compact {
    margin-top: 0.5rem;      /* ← 标题下方间距 */
    line-height: 1.8;        /* ← 行高（影响紧凑度）*/
}
```

#### B. 数字和单位之间的间距
```css
.countdown-compact .countdown-unit {
    margin-right: 0.5rem;    /* ← 单位右侧间距 */
}
```

#### C. 完整版倒计时（主页面）
```css
.countdown-display {
    gap: 1rem;               /* ← 天/时/分/秒之间的间距 */
}

.countdown-item {
    gap: 0.2rem;             /* ← 数值和单位之间的间距 */
}

.countdown-value {
    line-height: 1;          /* ← 数值行高 */
}

.countdown-unit {
    line-height: 1;          /* ← 单位行高 */
    margin-top: -0.2rem;     /* ← 向上微调 */
}
```

---

### 4️⃣ 待办事项侧栏内部间距

**文件**: `assets/pomodoro-todo.css`

#### A. 标题到内容的间距
```css
.todo-wrapper {
    margin-top: 0.6rem;      /* ← 标题下方间距 */
}
```

#### B. 分组区域
```css
.todo-groups-section {
    margin-bottom: 0.8rem;   /* ← 与主区域的间距 */
}

.todo-groups-header {
    margin-bottom: 0.5rem;   /* ← 标题与列表的间距 */
}

.todo-group-item {
    margin-bottom: 0.4rem;   /* ← 分组项之间的间距 */
}
```

#### C. 输入框区域
```css
.todo-input-group {
    margin-bottom: 0.8rem;   /* ← 与待办列表的间距 */
    gap: 0.5rem;             /* ← 输入框与按钮的间距 */
}
```

#### D. 待办列表
```css
.todo-item {
    margin-bottom: 0.5rem;   /* ← 待办项之间的间距 */
}

.todo-sub-item {
    margin-top: 0.3rem;      /* ← 子待办的顶部间距 */
    margin-left: 1.5rem;     /* ← 子待办的左侧缩进 */
}
```

---

### 5️⃣ 数据管理侧栏内部间距

**文件**: `assets/vocabulary.css`

#### A. 标题到内容的间距
```css
.data-management-wrapper {
    margin-top: 0.6rem;      /* ← 标题下方间距 */
}
```

#### B. 按钮之间的间距
```css
.data-management-buttons {
    gap: 0.5rem;             /* ← 导出和导入按钮之间的间距 */
}
```

---

## 🔧 快速调整指南

### 场景 1: 侧栏太挤，想增加间距

**调整侧栏之间的间距**:
```css
/* assets/pomodoro-todo.css */
.todo-margin-note {
    margin-top: 2rem !important;  /* 从 1.5rem 改为 2rem */
}

/* assets/vocabulary.css */
.data-management-margin-note {
    margin-top: 2rem !important;  /* 从 1.5rem 改为 2rem */
}
```

### 场景 2: 侧栏内容太松散，想紧凑一些

**调整单词学习侧栏**:
```css
/* assets/vocabulary.css */
.vocab-progress-section {
    margin-bottom: 0.5rem;  /* 从 0.8rem 改为 0.5rem */
}

.vocab-stats-section {
    padding-top: 0.5rem;    /* 从 0.8rem 改为 0.5rem */
    margin-top: 0.5rem;     /* 从 0.8rem 改为 0.5rem */
}
```

### 场景 3: 倒计时数字和单位太远

**调整倒计时间距**:
```css
/* assets/pomodoro-todo.css */
.countdown-item {
    gap: 0.1rem;            /* 从 0.2rem 改为 0.1rem */
}

.countdown-unit {
    margin-top: -0.3rem;    /* 从 -0.2rem 改为 -0.3rem */
}
```

### 场景 4: 待办事项太紧凑

**调整待办列表间距**:
```css
/* assets/pomodoro-todo.css */
.todo-item {
    margin-bottom: 0.8rem;  /* 从 0.5rem 改为 0.8rem */
}

.todo-groups-section {
    margin-bottom: 1.2rem;  /* 从 0.8rem 改为 1.2rem */
}
```

---

## 📊 间距值参考表

| 位置 | 当前值 | 推荐范围 | 说明 |
|------|--------|----------|------|
| 侧栏之间 | 1.5rem | 1-3rem | 太小会挤，太大会分散 |
| 标题到内容 | 0.6rem | 0.4-1rem | 标题下方的间距 |
| 区域之间 | 0.8rem | 0.5-1.2rem | 同一侧栏内不同区域 |
| 列表项之间 | 0.3-0.5rem | 0.2-0.8rem | 统计项、待办项等 |
| 按钮之间 | 0.5rem | 0.3-0.8rem | 并排按钮的间距 |
| 数字和单位 | 0.2rem | 0.1-0.5rem | 倒计时数字和单位 |

---

## 🎨 CSS 单位说明

- **rem**: 相对于根元素字体大小（通常 1rem = 16px）
- **0.5rem** ≈ 8px
- **0.6rem** ≈ 10px
- **0.8rem** ≈ 13px
- **1rem** ≈ 16px
- **1.5rem** ≈ 24px
- **2rem** ≈ 32px

---

## 🔍 调试技巧

### 1. 使用浏览器开发者工具
1. 右键点击侧栏元素 → "检查"
2. 在 Styles 面板中找到对应的 CSS 规则
3. 实时修改 `margin`、`padding`、`gap` 值查看效果
4. 满意后复制到对应的 CSS 文件

### 2. 添加临时边框查看布局
```css
/* 临时调试代码 */
.marginnote * {
    outline: 1px solid red;  /* 显示所有元素边界 */
}
```

### 3. 使用 CSS 变量统一管理
在 `assets/vocabulary.css` 或 `assets/pomodoro-todo.css` 顶部定义：
```css
:root {
    --sidebar-spacing-large: 1.5rem;   /* 侧栏之间 */
    --sidebar-spacing-medium: 0.8rem;  /* 区域之间 */
    --sidebar-spacing-small: 0.5rem;   /* 项目之间 */
    --sidebar-spacing-tiny: 0.3rem;    /* 紧凑间距 */
}
```

然后在样式中使用：
```css
.vocab-stats-section {
    margin-top: var(--sidebar-spacing-medium);
}
```

---

## 📝 修改流程

1. **确定要调整的位置** - 参考本文档找到对应的 CSS 文件和类名
2. **打开对应的 CSS 文件** - `vocabulary.css` 或 `pomodoro-todo.css`
3. **修改间距值** - 调整 `margin`、`padding`、`gap` 等属性
4. **重新构建网站** - 运行 `python build.py build`
5. **预览效果** - 运行 `python build.py preview` 或直接打开 HTML
6. **微调优化** - 根据实际效果继续调整

---

## 🎯 最佳实践

1. **保持一致性** - 相似的元素使用相同的间距值
2. **使用倍数关系** - 例如 0.4rem、0.8rem、1.6rem（便于记忆）
3. **移动端适配** - 在小屏幕上可能需要减小间距
4. **视觉层次** - 重要区域间距大，次要区域间距小
5. **测试不同内容** - 确保在内容多/少时都美观

---

**最后更新**: 2026-01-21  
**版本**: 1.0.0  
**作者**: Kiro AI Assistant
